<h1><%= @pet.name %></h1>

<p>Race: <%= @pet.race %></p>

<% if @pet.hurt? %>
  <p>💔</p>
<% else %>
  <p>❤️</p>
<% end %>

<%= image_tag @pet.imageUrl %>

<%= link_to "Edit the pettttt", edit_pet_path %> |
<%= link_to "Delete",
  pet_path(@pet),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
